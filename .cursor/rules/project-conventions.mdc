---
description: 项目全局规范，所有对话自动生效
alwaysApply: false
---

# 项目全局规范

## 语言
- 与用户沟通使用中文
- 代码注释使用英文
- Git commit message 使用英文

## 代码风格
- 优先使用已有的工具和库，不要重复造轮子
- 函数保持单一职责，不超过 50 行
- 变量命名清晰，不用缩写（`user_count` ✅ / `uc` ❌）

## 文件组织
- Python 文件放在项目根目录或对应模块目录
- 配置文件统一放在 `.cursor/` 下
- 文档统一放在 `docs/` 目录

## 错误处理
- 不允许空的 except/catch 块
- 错误信息要有上下文，方便排查
